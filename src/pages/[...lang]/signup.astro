---
export function getStaticPaths() {
  return [
    { params: { lang: undefined } },
    { params: { lang: "de" } },
  ];
}

const { lang } = Astro.params;
const target = lang === "de" ? "/de/onboarding" : "/onboarding";

return Astro.redirect(target, 301);
---
